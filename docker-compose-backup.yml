version: "3"
services:
  reactjs-app:
    # stdin_open: true #for -it flag alternative
    # tty: true
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src:ro
    environment:
      - REACT_APP_NAME=mhayreactjs
      - CHOKIDAR_USEPOLLING=true
    # env_file:
    #   - ./.env
